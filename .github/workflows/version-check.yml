# Generated by scitrera-repo-tools `generate-ci-gha`. Do not edit by hand;
# re-run the generator after editing versions.yaml. Run
# `generate-ci-gha` (no flags) to check for drift, `generate-ci-gha --force` to apply.

name: Version Check

on:
  pull_request:
    paths:
      - 'api/go.mod'
      - 'sdk/go/aether/version.go'
      - 'sdk/go/go.mod'
      - 'sdk/python-ag2/pyproject.toml'
      - 'sdk/python-ag2/scitrera_aether_ag2/__init__.py'
      - 'sdk/python-client/pyproject.toml'
      - 'sdk/python-client/scitrera_aether_client/__init__.py'
      - 'sdk/typescript/package.json'
      - 'server/go.mod'
      - 'server/internal/version/version.go'
      - 'versions.yaml'

permissions:
  contents: read

jobs:
  check:
    name: sync-versions --check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v9.0.0

      - name: Verify versions are in sync
        run: uvx --from 'scitrera-repo-tools==0.1.15' sync-versions --check --verbose

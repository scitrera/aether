# Core runtime dependencies
grpcio>=1.76.0
# Must not fall below the gencode version in proto/*_pb2.py (currently 6.33.5);
# the generated runtime guard rejects an older runtime. See versions.yaml.
protobuf>=6.33.5

# Development dependencies (also specified in pyproject.toml under the "dev" extra)
# For production installs, use: pip install scitrera-aether-client
# For development installs, use: pip install -e .[dev]
grpcio-tools>=1.81.1  # keep aligned with proto.toolchain.grpcio_tools in versions.yaml
pytest>=8.0.0
pytest-asyncio>=0.23.0
# Optional at runtime (requests_adapter / httpx_transport guard their imports),
# but required to run the test suite — their test modules import them directly.
requests>=2.32
httpx>=0.27
mypy>=1.8.0
types-protobuf>=4.24.0
grpc-stubs>=1.53.0
build>=1.0.0

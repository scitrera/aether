// Original file: sandbox_relay_tunnel.proto


/**
 * WatchTenantsRequest carries no parameters; the aggregator streams every
 * tenant transition for the lifetime of the call.
 */
export interface WatchTenantsRequest {
}

/**
 * WatchTenantsRequest carries no parameters; the aggregator streams every
 * tenant transition for the lifetime of the call.
 */
export interface WatchTenantsRequest__Output {
}
